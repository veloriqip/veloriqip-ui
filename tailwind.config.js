module.exports = {
  content: [
    './index.html',
    './src/**/*.{js,jsx,ts,tsx,vue}',
  ],
  theme: {
    // Ensure you haven't accidentally overridden the default color palette with an empty object
    // If you have theme.colors: {}, delete or modify it to extend the default colors
  },
  plugins: [],
}
